node_modules
.next
out
dist
.turbo
.vercel
.env
.env.local

packages/subgraph/buildapps/contracts/.env
apps/contracts/cache/
apps/contracts/script/K/

ngrok.yml

research

.claude
# macOS Finder writes these into any directory it displays. Never useful, and
# they reappear forever if only deleted rather than ignored.
.DS_Store
